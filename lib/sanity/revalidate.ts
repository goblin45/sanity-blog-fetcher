/**
 * Shared ISR window (seconds) for Sanity-backed public blog routes and
 * `sanityClient.fetch` Next.js cache options. Keep page `export const revalidate`
 * in sync with this value.
 */
// Added/Modified by Rajarshi for TBD
export const SANITY_REVALIDATE_SECONDS = 5;
